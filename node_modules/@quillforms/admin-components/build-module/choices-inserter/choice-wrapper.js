import _extends from "@babel/runtime/helpers/esm/extends";
import { createElement } from "@wordpress/element";
/**
 * QuillForms Dependencies
 */
import __experimentalDraggable from '../draggable';
import ChoiceRow from './choice-row';
const ChoiceWrapper = _ref => {
  let {
    index,
    style,
    data
  } = _ref;
  const item = data[index];

  // We are rendering an extra item for the placeholder
  if (item === undefined) {
    return null;
  }
  return createElement(__experimentalDraggable, {
    key: item.value,
    draggableId: item.value,
    index: index
  }, (provided, snapshot) => createElement("div", _extends({}, provided.draggableProps, {
    ref: provided.innerRef,
    "date-isDragging": snapshot.isDragging,
    style: {
      margin: 0,
      ...provided.draggableProps.style,
      ...style
    }
  }), createElement(ChoiceRow, {
    choices: data,
    index: index,
    provided: provided
  })));
};
export default ChoiceWrapper;
//# sourceMappingURL=choice-wrapper.js.map