/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  assetPrefix: './',
  basePath: "/accelar8_website",
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig;

module.exports = nextConfig
