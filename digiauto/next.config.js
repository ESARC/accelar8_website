/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  assetPrefix: './',
  basePath: "/digiauto",
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig;

module.exports = nextConfig
